# Plugins

This directory documents external plugin packages and integration contracts.

Core built-ins currently live under `internal/chain/*` and are surfaced via `pkg/pluginapi` for future extraction/versioning.
